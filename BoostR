using UnityEngine;
using System.Collections;

public class BoostR : MonoBehaviour
{

	public float speed = 1200;
	
	void OnTriggerEnter (Collider other)
	{
		other.rigidbody.AddForce(Vector3.right * speed, ForceMode.Acceleration);
	}
}
